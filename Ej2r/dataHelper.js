var datos = [];

/*Obtiene todos los elementos del array Datos*/
function getAll() {

    return datos;
}

/*Agregar un nuevo elemento al array Datos*/
function addNewItem(elem) {

    datos.push(elem);
}